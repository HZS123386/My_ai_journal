# AI 日记助手项目答辩文档

> 适用于毕业设计 / 课程设计 / 项目答辩 PPT  
> 项目主题：智能日记、个人复盘、AI 辅助记录  
> 建议答辩时长：8-12 分钟  

---

## 一、PPT 设计主题建议

### 1. 主题定位

本项目是一个“AI 日记助手”，因此 PPT 不建议做成传统冷冰冰的后台管理系统风格，而应突出：

- 日记本
- 夜间书写
- 情绪记录
- 时间沉淀
- AI 复盘

整体风格可以概括为：

> 静谧、温暖、轻科技、个人成长感

### 2. 推荐配色

| 用途 | 颜色 | 说明 |
|---|---|---|
| 主背景 | 深夜蓝黑 `#10131F` | 对应夜晚写日记的氛围 |
| 主色 | 紫色 `#A78BFA` | 对应 AI、智能、分析 |
| 辅色 | 粉色 `#F472B6` | 对应情绪、个人表达 |
| 高亮 | 蓝色 `#60A5FA` | 对应技术模块、接口 |
| 正文 | 浅灰白 `#E5E7EB` | 保持可读性 |
| 卡片背景 | 深灰蓝 `#171B2B` | 模拟项目中的毛玻璃卡片感 |

### 3. 页面风格

- 背景可以使用深色渐变或模糊纸张纹理
- 页面元素可以使用圆角卡片
- 图标可以使用：日记本、麦克风、时间线、AI 节点、数据库
- 每页不要堆满文字，重点词加粗或用色块突出
- 技术架构页用流程图，不要只列文字

---

## 二、答辩 PPT 正文内容

---

# 第 1 页：封面

## 页面标题

AI 日记助手

## 副标题

基于 FastAPI 与 AI 模型的智能日记管理系统

## 页面内容

- 记录每一天，AI 为你总结
- 支持日记记录、情绪识别、待办提取、周报生成、语音输入
- 答辩人：XXX
- 专业 / 班级：XXX
- 日期：2026 年

## 视觉建议

使用深色背景，中心放标题。右侧或底部可放一本打开的日记本、笔、麦克风或时间线元素。标题用紫粉渐变。

## 讲解词

各位老师好，我的项目名称是 AI 日记助手。它是一款面向个人日常记录和复盘的智能日记系统。项目的核心目标是让用户可以更轻松地记录每天发生的事情，并借助 AI 自动完成总结、情绪识别、待办提取以及周报生成。后续我会从项目背景、系统功能、技术架构、核心实现和测试总结几个方面进行介绍。

---

# 第 2 页：目录

## 页面标题

汇报目录

## 页面内容

1. 项目背景与意义
2. 需求分析
3. 系统功能设计
4. 技术架构设计
5. 数据库与接口设计
6. 核心功能实现
7. 语音识别模块实现
8. 测试与运行效果
9. 项目特色与不足
10. 总结与展望

## 视觉建议

用时间线或书签式目录。每一项像日记目录页中的章节标签。

## 讲解词

本次汇报主要分为十个部分。前半部分介绍为什么要做这个项目，以及系统需要解决哪些问题；中间部分重点介绍功能模块和技术架构；后半部分会讲核心实现，包括 AI 日记分析和本地语音识别模块，最后总结项目成果、不足和未来优化方向。

---

# 第 3 页：项目背景

## 页面标题

项目背景：为什么需要 AI 日记助手？

## 页面内容

### 传统日记存在的问题

- 记录门槛高：每天手动写长篇内容比较费时间
- 回顾成本高：历史日记内容多，难以快速找到重点
- 信息价值低：普通日记只是存储文字，缺少总结和分析
- 待办易遗漏：日记中提到的计划容易被忘记
- 情绪难追踪：用户很难长期观察自己的情绪变化

### 项目切入点

将 AI 能力引入日记系统，让日记不只是“存储文字”，而是变成一个可以帮助用户回顾、整理和规划的智能助手。

## 视觉建议

左边放“传统日记痛点”，右边放“AI 日记助手解决方案”，中间用箭头连接。

## 讲解词

传统日记虽然可以记录生活，但它更多是静态的文本存储。用户写完之后，后续回顾和整理的成本比较高。例如一篇日记里可能包含心情、计划和待办事项，但这些信息没有被结构化提取出来。本项目希望通过 AI 对日记内容进行理解，让系统自动提取摘要、情绪和待办，从而提升日记的实际使用价值。

---

# 第 4 页：项目目标

## 页面标题

项目目标

## 页面内容

本项目的目标是设计并实现一个个人智能日记系统，主要包括：

1. 用户可以注册、登录并安全管理自己的日记
2. 用户可以创建、查看、删除日记记录
3. 系统可以调用 AI 对日记内容进行分析
4. 系统可以自动生成摘要、情绪标签和待办事项
5. 系统可以根据最近日记生成本周周报
6. 系统支持文件上传和内容总结
7. 系统支持语音输入，将录音转写为日记文本

## 视觉建议

用 7 个功能图标围绕中心的“AI 日记助手”。

## 讲解词

项目目标可以分为两个层面。第一层是基础日记管理，包括用户认证、日记增删查以及历史记录展示。第二层是智能化能力，包括 AI 总结、情绪识别、待办提取、周报生成、文件总结以及语音输入。通过这两层功能，系统既具备完整的日记管理能力，也具备智能辅助能力。

---

# 第 5 页：需求分析

## 页面标题

需求分析

## 页面内容

### 功能需求

| 模块 | 需求说明 |
|---|---|
| 用户模块 | 注册、登录、JWT 身份认证 |
| 日记模块 | 创建日记、查看历史、删除日记 |
| AI 分析模块 | 摘要生成、情绪识别、待办提取 |
| 周报模块 | 汇总近期日记，生成周总结 |
| 文件模块 | 上传文档并生成内容总结 |
| 语音模块 | 录音或上传音频，转写为文本 |

### 非功能需求

- 安全性：密码加密、接口认证
- 易用性：页面简洁、操作反馈明确
- 可扩展性：AI 服务模块独立封装
- 可维护性：后端分层清晰，代码结构明确

## 视觉建议

表格右侧可以放系统角色图：用户、前端、后端、AI 服务、数据库。

## 讲解词

在需求分析阶段，我将系统需求分成功能需求和非功能需求。功能需求主要围绕用户使用流程展开，包括登录、写日记、查看日记、AI 分析和语音输入。非功能需求主要关注系统安全、用户体验和后续扩展。例如 AI 服务是独立封装在 `ai_service.py` 中的，后续如果更换模型或服务商，只需要改这一层。

---

# 第 6 页：系统整体功能

## 页面标题

系统功能模块

## 页面内容

系统主要由 6 个模块组成：

1. 用户认证模块
2. 日记管理模块
3. AI 日记分析模块
4. 周报生成模块
5. 文件上传总结模块
6. 本地语音识别模块

### 功能流程

用户登录后，可以输入文字日记，也可以通过语音输入生成文本。提交后，系统调用 AI 分析模块生成摘要、情绪和待办，并保存到数据库。用户可以查看历史记录，也可以生成近期周报。

## 视觉建议

画一个功能模块图，中心是“AI 日记助手”，四周是各模块。

## 讲解词

系统功能围绕“记录”和“复盘”展开。用户可以通过文字或语音创建日记，系统对日记内容进行 AI 分析，然后将结构化结果保存下来。历史记录模块用于查看过往内容，周报模块用于阶段性总结，文件上传模块则扩展了系统对外部文档内容的处理能力。

---

# 第 7 页：技术栈

## 页面标题

技术选型

## 页面内容

| 层级 | 技术 | 作用 |
|---|---|---|
| 后端框架 | FastAPI | 提供 REST API 与页面路由 |
| 前端页面 | Jinja2 + HTML/CSS/JavaScript | 实现交互界面 |
| 数据库 | SQLite | 本地轻量级数据存储 |
| ORM | SQLAlchemy | 数据库对象映射 |
| 用户认证 | JWT + bcrypt | 登录认证与密码加密 |
| AI 文本分析 | DeepSeek API | 日记总结、情绪识别、待办提取 |
| 本地语音识别 | faster-whisper small | 音频转文字 |
| 测试 | pytest | 冒烟测试与路由验证 |

## 视觉建议

使用分层技术栈图，从上到下依次是前端、后端、AI、数据库、测试。

## 讲解词

技术选型上，后端使用 FastAPI，因为它性能较好、开发效率高，并且天然支持接口文档。数据库使用 SQLite，适合个人项目和课程设计场景。AI 文本分析使用 DeepSeek API，语音识别采用本地 faster-whisper small 模型。这样文本理解和语音转写职责清晰，也便于后续替换或扩展。

---

# 第 8 页：系统架构设计

## 页面标题

系统架构设计

## 页面内容

```text
用户浏览器
   |
   |  HTML / CSS / JavaScript
   v
FastAPI 后端服务
   |
   |-- 用户认证：JWT、bcrypt
   |-- 日记接口：/entries
   |-- 周报接口：/weekly-report
   |-- 文件接口：/upload-file
   |-- 语音接口：/speech/transcribe
   |
   |-- AI 服务层：DeepSeek、faster-whisper
   |
   v
SQLite 数据库
```

## 视觉建议

这一页建议画正式架构图。左侧用户，中心 FastAPI，右侧 AI 服务，下方 SQLite 数据库。

## 讲解词

系统采用前后端结合的架构。前端通过 Jinja2 模板渲染页面，并使用 JavaScript 调用后端接口。后端 FastAPI 负责业务逻辑，包括认证、日记管理、文件处理和语音转写。AI 服务层被封装在独立模块中，其中 DeepSeek 负责文本分析，faster-whisper 负责本地语音识别。数据最终存储在 SQLite 数据库中。

---

# 第 9 页：项目目录结构

## 页面标题

项目结构

## 页面内容

```text
ai_journal/
├── app.py                  # FastAPI 主应用与路由
├── ai_service.py           # AI 文本分析与语音识别
├── auth.py                 # 密码加密与 JWT 生成校验
├── database.py             # 数据库连接配置
├── models.py               # SQLAlchemy 数据模型
├── schemas.py              # Pydantic 数据校验模型
├── file_parser.py          # 文件解析模块
├── templates/
│   ├── index.html          # 日记主页
│   └── upload.html         # 文件上传页面
├── static/
│   ├── style.css           # 主页样式
│   └── upload.css          # 上传页样式
├── tests/
│   └── test_smoke.py       # 冒烟测试
└── requirements.txt        # 依赖列表
```

## 视觉建议

可以用代码目录树截图或重新绘制目录树卡片。

## 讲解词

项目结构比较清晰。`app.py` 是后端入口，负责路由注册和请求处理；`models.py` 和 `schemas.py` 分别负责数据库模型和接口数据结构；`auth.py` 处理用户认证；`ai_service.py` 负责 AI 分析和语音识别；前端页面放在 `templates` 中，样式放在 `static` 中，测试代码放在 `tests` 中。

---

# 第 10 页：数据库设计

## 页面标题

数据库设计

## 页面内容

### User 用户表

| 字段 | 说明 |
|---|---|
| id | 用户 ID |
| username | 用户名 |
| email | 邮箱 |
| password_hash | 加密后的密码 |
| created_at | 创建时间 |

### Entry 日记表

| 字段 | 说明 |
|---|---|
| id | 日记 ID |
| content | 日记原文 |
| summary | AI 总结 |
| mood | 情绪标签 |
| todos | 待办事项 JSON |
| created_at | 创建时间 |
| user_id | 所属用户 |

## 视觉建议

画 ER 图：User 1 对多 Entry。

## 讲解词

数据库主要包含用户表和日记表。用户表保存用户基础信息和密码哈希，不保存明文密码。日记表保存用户提交的原文，以及 AI 分析后的摘要、情绪和待办事项。用户和日记之间是一对多关系，一个用户可以拥有多条日记记录。

---

# 第 11 页：用户认证设计

## 页面标题

用户认证与安全设计

## 页面内容

### 认证流程

1. 用户注册时提交用户名、邮箱和密码
2. 后端使用 bcrypt 对密码进行哈希存储
3. 用户登录时校验邮箱和密码
4. 登录成功后生成 JWT Token
5. 前端将 Token 保存在 localStorage
6. 访问受保护接口时携带 Authorization Header
7. 后端解析 Token 并确认当前用户身份

### 安全点

- 不存储明文密码
- 日记接口必须登录后访问
- 每个用户只能访问自己的日记

## 视觉建议

用登录流程图：注册/登录 -> token -> 访问接口 -> 返回用户数据。

## 讲解词

用户认证模块主要使用 JWT 和 bcrypt。bcrypt 用于密码加密，避免数据库中出现明文密码。JWT 用于登录状态保持，用户登录后前端会保存 token，之后访问 `/entries`、`/weekly-report`、`/speech/transcribe` 等接口时，需要携带这个 token。后端会根据 token 判断当前用户身份，从而保证数据隔离。

---

# 第 12 页：日记管理实现

## 页面标题

日记管理模块

## 页面内容

### 核心接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/entries` | POST | 创建新日记 |
| `/entries` | GET | 获取当前用户日记列表 |
| `/entries/{entry_id}` | DELETE | 删除指定日记 |

### 实现逻辑

创建日记时：

1. 前端获取输入框内容
2. 携带 token 提交到 `/entries`
3. 后端校验用户身份
4. 调用 AI 分析服务
5. 保存原文、摘要、情绪和待办
6. 返回保存结果并刷新列表

## 视觉建议

用流程图展示“输入日记 -> AI 分析 -> 数据库存储 -> 页面展示”。

## 讲解词

日记管理模块是系统的核心业务。用户在主页输入日记内容后，前端会调用 `/entries` 接口。后端首先校验用户身份，然后调用 AI 服务对内容进行分析。最终保存的不只是原始文本，还包括摘要、情绪和待办事项。这样用户查看历史记录时，可以快速理解每篇日记的重点。

---

# 第 13 页：AI 日记分析模块

## 页面标题

AI 日记分析模块

## 页面内容

### 输入

用户提交的一段中文日记文本。

### 输出

```json
{
  "summary": "一句话总结",
  "mood": "平静",
  "todos": ["待办1", "待办2"]
}
```

### 分析内容

- `summary`：提炼日记主要内容
- `mood`：从 开心、焦虑、平静、疲惫、沮丧 中选择
- `todos`：提取日记中明确提到的待办事项

## 视觉建议

左边是“日记原文”，中间是“AI 分析”，右边是“结构化结果”。

## 讲解词

AI 分析模块的作用是把用户的自然语言日记转换成结构化信息。系统会要求模型严格返回 JSON，包括总结、情绪和待办事项。这样做的好处是后端可以稳定解析模型输出，并把结果存入数据库。相比普通文本存储，这种结构化结果更适合后续展示、统计和周报生成。

---

# 第 14 页：周报生成模块

## 页面标题

本周周报生成

## 页面内容

### 功能目标

帮助用户从近期日记中进行阶段性复盘。

### 实现流程

1. 查询当前用户最近 7 条日记
2. 整理每条日记的原文、摘要、情绪和待办
3. 调用 AI 生成周报内容
4. 返回：
   - 本周总结
   - 情绪概览
   - 关键待办
   - 下周建议

## 视觉建议

用周历卡片样式，展示 7 天记录汇总成一份周报。

## 讲解词

周报模块体现了日记系统的复盘价值。系统会取当前用户最近的日记，将这些内容组织成统一格式，再交给 AI 生成周报。周报不仅包括对一周情况的总结，还会分析情绪变化、提取重要待办，并给出下周建议。这样用户可以从零散记录中看到整体状态。

---

# 第 15 页：文件上传总结模块

## 页面标题

文件上传与内容总结

## 页面内容

### 支持格式

- TXT
- DOCX
- XLSX
- PDF

### 实现逻辑

1. 用户上传文件
2. 后端校验文件格式
3. 根据文件类型解析文本内容
4. 调用 AI 生成文档总结
5. 返回摘要、关键点和文档类别

### 扩展意义

该模块让系统不仅能处理日记输入，也可以处理外部文档内容，扩展了应用场景。

## 视觉建议

使用文件图标和解析流程图。

## 讲解词

文件上传模块是系统的扩展功能。用户可以上传常见文档格式，后端通过 `file_parser.py` 解析文件内容，再调用 AI 生成总结和关键点。这个模块和日记分析模块使用了类似的 AI 调用方式，说明项目的 AI 服务具有一定复用性。

---

# 第 16 页：语音识别模块设计

## 页面标题

语音输入模块设计

## 页面内容

### 为什么增加语音输入？

- 降低日记记录门槛
- 用户可以用口述方式快速记录
- 更符合移动端和碎片化记录场景

### 技术方案

采用：

```text
浏览器录音 / 上传音频
        ↓
FastAPI 接收音频
        ↓
faster-whisper small 本地转写
        ↓
文本填入日记输入框
        ↓
复用原有日记提交流程
```

## 视觉建议

用麦克风图标、声波、文本框转换图。

## 讲解词

语音输入是项目后期增加的功能，目的是降低用户记录日记的门槛。用户不一定每次都愿意打字，所以系统提供了录音和上传音频两种方式。前端采集音频后提交给后端，后端使用本地 faster-whisper small 模型完成语音转文字，然后把结果填入日记输入框。之后的日记提交仍然复用原来的 `/entries` 流程。

---

# 第 17 页：为什么选择 faster-whisper

## 页面标题

本地语音识别方案：faster-whisper small

## 页面内容

### 选择原因

| 维度 | 说明 |
|---|---|
| 免费使用 | 本地模型，无需按 API 次数付费 |
| 隐私更好 | 音频在本地服务端处理，不上传第三方语音接口 |
| Python 易接入 | 可以直接集成到 FastAPI 后端 |
| 中文效果较好 | Whisper 系列对中文转写支持较好 |
| small 模型均衡 | 准确率和资源消耗相对平衡 |

### 配置

```env
WHISPER_MODEL_SIZE=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
WHISPER_LANGUAGE=zh
```

## 视觉建议

用“云端 API”与“本地模型”对比图。本项目选择本地模型路线。

## 讲解词

语音识别有两类方案，一类是调用云端语音 API，另一类是使用本地开源模型。这里我选择 faster-whisper small，是因为它可以免费本地运行，避免语音数据上传到第三方接口，也不会产生语音 API 调用费用。small 模型在识别效果和本地运行成本之间比较均衡，适合本项目。

---

# 第 18 页：语音识别接口实现

## 页面标题

语音识别接口实现

## 页面内容

### 接口

```text
POST /speech/transcribe
```

### 请求

- `Authorization: Bearer <token>`
- `file`: 音频文件

### 后端处理

1. 校验登录 token
2. 检查音频格式
3. 检查文件大小，最大 25MB
4. 写入临时文件
5. 调用 faster-whisper 模型转写
6. 删除临时文件
7. 返回文本结果

### 支持格式

`flac`、`mp3`、`mp4`、`mpeg`、`mpga`、`m4a`、`ogg`、`wav`、`webm`

## 视觉建议

画接口调用流程图，突出“认证、校验、转写、清理、返回”。

## 讲解词

语音识别接口和其他核心接口一样，需要登录后才能访问。这样可以保证只有当前用户能使用该功能。接口会先校验音频格式和大小，然后把音频保存为临时文件。faster-whisper 根据临时文件路径进行转写，转写完成后立即删除临时文件，最后返回识别出的文本。

---

# 第 19 页：前端交互设计

## 页面标题

前端交互设计

## 页面内容

### 主页输入区

- 文本框：输入日记内容
- 语音输入按钮：录音并自动识别
- 上传录音按钮：选择已有音频文件
- 状态提示：显示录音、上传、识别结果状态
- 提交按钮：提交最终日记文本

### 用户体验设计

- 录音中按钮显示状态变化
- 识别失败时弹出错误提示
- 识别成功后自动追加到日记文本框
- 用户仍然可以手动修改识别结果

## 视觉建议

放主页截图，重点框出语音输入区域。

## 讲解词

前端交互上，我没有让语音识别直接提交日记，而是先把识别结果填入文本框。这样用户可以在提交前进行检查和修改，避免语音识别错误直接保存。录音、上传、识别过程都有状态提示，让用户知道当前系统正在做什么。

---

# 第 20 页：界面设计

## 页面标题

界面设计与主题适配

## 页面内容

### 设计风格

- 深色背景：营造夜间书写和私人空间感
- 紫粉渐变：体现 AI 与情绪记录
- 卡片式布局：突出内容分区
- 响应式设计：适配不同屏幕尺寸
- 深色 / 浅色主题切换：满足不同使用偏好

### 主要页面

- 登录 / 注册页面
- 日记主页
- 历史记录侧边栏
- 文件上传页面
- 周报展示区域

## 视觉建议

展示项目主页和上传页截图。背景使用项目里的深色风格。

## 讲解词

由于项目主题是日记，所以界面设计上我希望它更像一个个人记录空间，而不是传统管理系统。项目采用深色背景、紫粉色高亮和卡片式布局，让整体视觉更贴近日记、情绪和 AI 智能分析的主题。同时系统支持主题切换，提升了用户体验。

---

# 第 21 页：测试与验证

## 页面标题

测试与验证

## 页面内容

### 测试方式

使用 `pytest` 编写冒烟测试，验证：

- FastAPI 应用可以正常导入
- 应用标题正确
- 核心路由存在
- `/entries` 日记接口存在
- `/weekly-report` 周报接口存在
- `/speech/transcribe` 语音识别接口存在

### 测试命令

```bash
.venv\Scripts\python.exe -m pytest -q
```

### 当前结果

```text
2 passed
```

## 视觉建议

可以放测试终端截图，右侧列出测试通过项。

## 讲解词

测试方面，我使用 pytest 对项目进行了基础冒烟测试。虽然测试规模不算大，但它可以保证项目应用能正常导入，并且核心路由没有丢失。尤其是在新增语音识别接口后，我也把 `/speech/transcribe` 加入了测试范围，保证功能变更后基础路由仍然正常。

---

# 第 22 页：项目特色

## 页面标题

项目特色与创新点

## 页面内容

### 1. 日记内容结构化

系统不仅保存原文，还自动生成摘要、情绪和待办。

### 2. 记录方式更灵活

支持文字输入、语音输入和文件上传。

### 3. AI 能力模块化

文本分析和语音转写都封装在 AI 服务层，后续可替换模型。

### 4. 本地语音识别

使用 faster-whisper small，不依赖付费语音 API。

### 5. 周报复盘

帮助用户从多篇日记中获得阶段性总结。

## 视觉建议

用五个高亮卡片展示特色，每个卡片一个关键词。

## 讲解词

本项目的特色主要体现在两个方面。第一是日记内容结构化，系统能从普通文本中提取摘要、情绪和待办。第二是输入方式灵活，用户可以通过文字、文件或语音记录内容。尤其是语音识别模块采用本地 faster-whisper 模型，不依赖付费云端接口，增强了项目的实用性和可控性。

---

# 第 23 页：遇到的问题与解决方案

## 页面标题

问题与解决方案

## 页面内容

| 问题 | 解决方案 |
|---|---|
| AI 输出格式不稳定 | 提示词要求严格返回 JSON，并做 JSON 清洗 |
| 用户数据隔离 | 所有日记接口都依赖当前登录用户 |
| 文件格式多样 | 针对不同文件格式使用解析模块 |
| 浏览器语音识别兼容性不稳定 | 改为后端本地 faster-whisper 统一识别 |
| Windows 临时文件读取问题 | 语音文件先写入临时路径，转写后删除 |
| 语音模型首次加载较慢 | 使用懒加载，只在第一次识别时加载模型 |

## 视觉建议

用“问题 -> 方案”的双列表。

## 讲解词

开发过程中遇到了一些问题。例如 AI 模型有时会返回非标准 JSON，因此我在提示词中要求严格返回 JSON，并在后端做了简单清洗。语音识别方面，浏览器自带识别能力兼容性不好，所以我改成后端统一使用 faster-whisper。为了避免应用启动慢，语音模型采用懒加载，只在第一次使用时加载。

---

# 第 24 页：项目不足

## 页面标题

项目不足

## 页面内容

当前项目仍有一些可以改进的地方：

1. 测试覆盖还不够全面，目前主要是冒烟测试
2. SQLite 更适合单机使用，高并发场景需要更换 PostgreSQL
3. 语音识别首次使用需要下载模型，启动体验可以继续优化
4. 情绪分析目前是固定标签，后续可以做更细粒度分类
5. 日记搜索和统计图表功能还可以继续完善
6. 前端目前是模板加原生 JavaScript，复杂度提升后可考虑组件化

## 视觉建议

用“当前版本”到“未来版本”的路线图。

## 讲解词

项目目前已经实现了完整的主流程，但仍有一些不足。比如测试覆盖还可以继续提升，数据库在高并发场景下需要升级，语音模型首次加载速度也可以继续优化。前端目前采用模板和原生 JavaScript，如果未来功能继续增加，可以考虑用 Vue 或 React 进行组件化重构。

---

# 第 25 页：未来展望

## 页面标题

未来展望

## 页面内容

### 功能扩展

- 日记搜索
- 情绪趋势图
- 日历视图
- 图片日记
- 标签分类
- 导出 PDF / Word

### AI 能力提升

- 更细粒度的情绪分析
- 个性化写作建议
- 长期成长报告
- 多模态日记分析

### 工程优化

- 增加单元测试和接口测试
- 数据库升级到 PostgreSQL
- 前端组件化
- 部署到云服务器

## 视觉建议

做成三段路线图：短期、中期、长期。

## 讲解词

未来可以从功能、AI 能力和工程质量三个方向继续优化。功能上可以增加搜索、日历、标签和导出。AI 能力上可以做更细粒度的情绪分析和长期成长报告。工程上可以增加测试覆盖，升级数据库，并将前端组件化，使项目更加稳定和可扩展。

---

# 第 26 页：总结

## 页面标题

项目总结

## 页面内容

本项目完成了一个完整的 AI 日记助手系统：

- 实现了用户注册、登录和身份认证
- 实现了日记创建、展示和删除
- 接入 DeepSeek 完成日记摘要、情绪识别和待办提取
- 实现了本周周报生成
- 支持文件上传与文档总结
- 新增本地 faster-whisper 语音输入能力
- 使用 pytest 完成基础测试验证

### 核心价值

让日记从“文字记录”升级为“智能复盘工具”。

## 视觉建议

用一句中心口号收尾：记录每一天，AI 为你总结。

## 讲解词

总体来看，本项目实现了从用户认证、日记管理到 AI 分析和语音输入的完整流程。它不仅能保存用户日记，还能自动提炼内容价值，帮助用户更好地进行日常复盘。通过这个项目，我对 FastAPI 后端开发、数据库设计、JWT 认证、AI 服务接入以及本地语音识别都有了更完整的实践。

---

# 第 27 页：致谢

## 页面标题

感谢聆听

## 页面内容

AI 日记助手  
记录每一天，AI 为你总结

欢迎各位老师批评指正。

## 视觉建议

背景可以使用深色日记本插画或项目主页截图，文字保持简洁。

## 讲解词

我的汇报到这里结束。感谢各位老师的聆听，欢迎老师们批评指正。

---

## 三、答辩常见问题准备

### Q1：为什么使用 FastAPI？

FastAPI 开发效率高，性能较好，支持类型注解和自动接口文档，适合快速构建 REST API。本项目接口较多，例如登录、日记、周报、文件上传、语音识别等，使用 FastAPI 可以让后端结构更加清晰。

### Q2：为什么使用 SQLite？

SQLite 部署简单，不需要单独安装数据库服务，适合个人项目、课程设计和小规模应用。如果后续部署到生产环境或用户量增加，可以迁移到 PostgreSQL。

### Q3：AI 分析结果如何保证格式稳定？

通过提示词要求模型严格返回 JSON，并在后端对模型输出进行清洗和解析，例如去除代码块标记、截取 JSON 范围等。这样可以降低模型输出格式不稳定带来的影响。

### Q4：为什么语音识别不用浏览器自带 Web Speech API？

浏览器自带语音识别 API 兼容性不稳定，不同浏览器支持情况不一致，而且识别能力不可控。因此项目改为后端统一处理，使用本地 faster-whisper 模型完成转写，更加可控。

### Q5：为什么选择 faster-whisper small？

faster-whisper 是 Whisper 的高性能实现，Python 接入方便。small 模型在准确率和运行成本之间比较平衡，适合中文日记转写场景。如果设备性能更好，可以替换为 medium 或 large-v3。

### Q6：语音识别会不会泄露隐私？

当前方案是本地 faster-whisper 转写，音频由项目后端处理，不需要上传到第三方语音识别 API。后端只使用临时文件进行转写，完成后删除，因此隐私性比云端语音 API 更好。

### Q7：项目如何保证用户只能看到自己的日记？

后端接口通过 JWT 获取当前用户身份，并在查询日记时使用 `user_id` 过滤。删除日记时也会同时判断日记 ID 和当前用户 ID，防止越权访问。

### Q8：项目最大的亮点是什么？

项目最大的亮点是把普通日记系统升级成智能复盘系统。它不仅能记录文本，还能自动总结、识别情绪、提取待办、生成周报，并支持语音输入。

---

## 四、推荐演讲时间分配

| 部分 | 时间 |
|---|---|
| 项目背景与目标 | 1 分钟 |
| 需求与功能模块 | 1.5 分钟 |
| 技术架构与数据库 | 2 分钟 |
| AI 分析模块 | 1.5 分钟 |
| 语音识别模块 | 2 分钟 |
| 测试、特色与总结 | 2 分钟 |

---

## 五、精简版 10 分钟答辩主线

如果答辩时间有限，可以按下面主线讲：

1. 我做的是一个 AI 日记助手，目标是让日记从文字记录变成智能复盘工具。
2. 系统支持登录、写日记、查看历史、删除日记。
3. 用户提交日记后，系统调用 DeepSeek 自动生成摘要、情绪和待办。
4. 系统还能根据最近日记生成周报，帮助用户阶段性总结。
5. 我新增了语音输入功能，前端录音，后端用 faster-whisper small 本地转写。
6. 本地语音识别的优势是不依赖付费语音 API，隐私性更好。
7. 后端使用 FastAPI，数据库使用 SQLite，认证使用 JWT 和 bcrypt。
8. 项目通过 pytest 做了基础路由测试。
9. 项目的不足是测试覆盖和数据可视化还可以继续增强。
10. 总结来说，本项目完成了从记录、分析到复盘的完整闭环。

